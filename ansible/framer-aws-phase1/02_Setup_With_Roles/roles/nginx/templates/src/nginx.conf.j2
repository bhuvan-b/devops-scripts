events {}

http {
    server {
        listen {{ nginx_http_port }};

        root {{ nginx_root_dir }};   
        index index.html index.htm; 

        location / {
            try_files $uri $uri/ =404;  
        }
    }
}
